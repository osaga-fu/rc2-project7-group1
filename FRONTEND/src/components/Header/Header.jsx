import  AddItemModal  from "../Modal/AddItemModal";
import { Logo } from "../Logo/Logo";

export const Header = () => {
  return (
    <header>
      <Logo />
      <AddItemModal />
    </header>
  );
};
